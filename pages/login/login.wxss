/* pages/login/login.wxss */
/* filepath: d:\LabReservation\pages\login\login.wxss */
/* pages/login/login.wxss */

.page-container {
    display: flex;
    flex-direction: column;
    align-items: center; /* 水平居中 */
    justify-content: center; /* 垂直居中内容（如果希望内容在屏幕中间） */
    padding: 40rpx;
    box-sizing: border-box;
    min-height: 100vh; /* 使容器至少占据整个屏幕高度 */
    background-color: #f8f8f8; /* 添加浅灰色背景 */
  }
  
  .form-section {
    width: 100%; /* 表单区域宽度占满容器 */
    background-color: #ffffff; /* 白色背景 */
    padding: 40rpx 30rpx; /* 内边距 */
    border-radius: 16rpx; /* 圆角 */
    box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05); /* 添加轻微阴影 */
    margin-bottom: 50rpx; /* 与下方按钮区域的间距 */
  }
  
  /* 为 t-input 组件之间添加一些间距 */
  /* 注意：这可能需要根据 t-input 的实际渲染结构调整 */
  /* 如果 t-input 外层没有包裹元素，可能需要给第二个 t-input 添加上边距 */
  .form-section t-input:not(:first-child) {
    margin-top: 30rpx;
  }
  
  .button-section {
    width: 100%; /* 按钮区域宽度占满容器 */
    display: flex;
    flex-direction: column; /* 按钮和链接垂直排列 */
    align-items: center; /* 内部元素居中 */
  }
  
  /* 登录按钮样式 */
  .login-button {
    width: 100%; /* 按钮宽度占满 */
    margin-bottom: 30rpx; /* 与注册链接的间距 */
    /* t-button 默认有主题色，这里可以覆盖或添加其他样式 */
    /* 例如： height: 90rpx; line-height: 90rpx; */
  }
  
  /* 注册链接样式 */
  .register-navigator {
    font-size: 28rpx;
    color: #0052d9; /* 使用 TDesign 主题蓝色 */
    text-decoration: none; /* 通常不需要下划线，但 navigator 默认可能有 */
  }
  
  .navigator-hover {
    color: #0044b3; /* 点击时的颜色变化 */
    text-decoration: underline; /* 点击时显示下划线 */
  }
  